# Experiment configs (Directive #9)

Pre-registered / frozen experiment YAML for historical text validation.

- `sentiment_historical_text_study_v1.yaml` — status starts as
  `not-yet-frozen-for-holdout`; set to `frozen-for-holdout` only after
  formation+validation and a FINAL CONFIGURATION FROZEN record on Issue #3.
